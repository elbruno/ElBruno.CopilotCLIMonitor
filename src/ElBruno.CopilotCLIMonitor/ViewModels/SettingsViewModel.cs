namespace ElBruno.CopilotCLIMonitor.ViewModels;

/// <summary>
/// ViewModel for Settings window.
/// Currently a placeholder awaiting Dolph's CoreSettings API from Issue #5 (logging merge).
/// Will bind IPC Configuration, Event Store Settings, and Logging Configuration once APIs are available.
/// </summary>
public class SettingsViewModel : ViewModelBase
{
    public SettingsViewModel()
    {
        // TODO: Inject CoreSettings once Dolph's logging APIs are available
    }
}
